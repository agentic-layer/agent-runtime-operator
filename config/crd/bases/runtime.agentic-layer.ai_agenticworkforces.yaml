---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: agenticworkforces.runtime.agentic-layer.ai
spec:
  group: runtime.agentic-layer.ai
  names:
    kind: AgenticWorkforce
    listKind: AgenticWorkforceList
    plural: agenticworkforces
    singular: agenticworkforce
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner
      name: Owner
      type: string
    - jsonPath: .spec.entryPointAgents[*]
      name: Entry Points
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgenticWorkforce is the Schema for the agenticworkforces API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgenticWorkforceSpec defines the desired state of AgenticWorkforce
            properties:
              description:
                description: Description provides details about the workforce's purpose
                  and capabilities
                type: string
              entryPointAgents:
                description: EntryPointAgents defines references to the entry-point
                  agents for this workforce
                items:
                  description: AgentReference defines a reference to an entry-point
                    agent
                  properties:
                    name:
                      description: Name is the name of the Agent resource
                      type: string
                    namespace:
                      description: Namespace is the namespace of the Agent resource
                        (optional, defaults to same namespace as AgenticWorkforce)
                      type: string
                  required:
                  - name
                  type: object
                type: array
              name:
                description: Name is the human-readable name of the workforce
                type: string
              owner:
                description: Owner is the email address or identifier of the workforce
                  owner
                type: string
              tags:
                description: Tags is a list of labels for categorizing and organizing
                  workforces
                items:
                  type: string
                type: array
            required:
            - description
            - entryPointAgents
            - name
            - owner
            type: object
          status:
            description: AgenticWorkforceStatus defines the observed state of AgenticWorkforce
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the workforce's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              transitiveAgents:
                description: TransitiveAgents contains all agents (both cluster and
                  remote) discovered from entry-point agents
                items:
                  type: string
                type: array
              transitiveTools:
                description: TransitiveTools contains all tools discovered from all
                  transitive agents
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
